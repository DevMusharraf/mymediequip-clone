import React from 'react'
import Navbar from '../components/navbar'; // Adjust the file names and paths as needed
import Login from '../components/login';   // Adjust the file names and paths as needed
import Footer from '../components/Footer'; // Adjust the file names and paths as needed


function User 
() {
  return (
    <div>
        <Navbar />
        <Login />
        <Footer />
    </div>
  )
}

export default User
